apiVersion: v1
kind: Pod
metadata:
  name: packager
spec:
  containers:
  - name: jnlp
    image: jenkins/inbound-agent:latest
  - name: manifest-tool
    image: mplatform/manifest-tool:alpine-v2.1.6
    command:
    - cat
    tty: true
