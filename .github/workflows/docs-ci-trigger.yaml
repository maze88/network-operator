name: Documentation CI - call downstream workflow

on:
  push:
    tags:
    - 'v*'

jobs:
  trigger_downstream_workflow:
    uses: maze88/network-operator-docs/.github/workflows/docs-ci.yaml@feature/make-docs  # TODO: revert owner to Mellanox after testing and merging
    with:
      git_tag: ${{ github.ref_name }}
    secrets: inherit
